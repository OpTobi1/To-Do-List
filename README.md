# Java To-Do List with File Storage

A simple Command Line Interface (CLI) application to manage your daily tasks.

## Features
* Add, view, and remove tasks.
* **Auto-Save**: Tasks are automatically saved to a `tasks.txt` file and reloaded when you start the app.
* Built with Java Collections and I/O streams.
